# _smoke-hello

Tooling smoke scenario for the corpus harness. Verifies that `Discover`,
`RunMask`, `Canonicalize`, and `Compare` round-trip cleanly on input that
contains no maskable tokens. Not a real regression case - bootstrap (U5)
may keep or remove this directory.
