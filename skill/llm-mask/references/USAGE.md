# llm-mask usage reference

Use `llm-mask mask` on raw logs before analysis. Preserve tokens exactly in
intermediate notes and final reports. Leave restoration to the human with
`llm-mask unmask < report.md`.
