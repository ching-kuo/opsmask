# opsmask usage reference

Use `opsmask mask` on raw logs before analysis. Preserve tokens exactly in
intermediate notes and final reports. Leave restoration to the human with
`opsmask unmask < report.md`.
